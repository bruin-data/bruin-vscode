/**
 * Universal click interceptor fix for VS Code extension tests
 * This enhances error handling for modal dialogs
 */

// Simple module to ensure imports work correctly
export const CLICK_INTERCEPTOR_LOADED = true;